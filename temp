obj <- lsf.str()
obj <- obj[!(obj %in% c('AR', 'Gamma', 'periods'))]

RES <- list()

for(h in 1:35) {
  q.sim <- AR(dta.cal[,Q], h)

  RES[[h]] <- sapply(obj, function(x) {do.call(x, 
                                          list(Qmer = dta.cal[h:.N,Q], 
                                               Qsim = q.sim))})
}

res <- do.call(rbind, RES)


